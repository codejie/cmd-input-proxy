{
  "name": "cmd-input-server",
  "version": "1.0.0",
  "description": "socket server is used to receive the command input from telnet client.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jie.",
  "license": "ISC",
  "dependencies": {
    "poplar-logger": "^1.0.3"
  }
}
