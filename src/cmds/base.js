'use strict';

class Base {
    constructor() {
    }

    parser (parser) {
        this.parser = parser;
    }

    handle (conn, args) {

    }

    // usage (args) {
    //     return ('no usage info.');        
    // }
}

module.exports = Base;